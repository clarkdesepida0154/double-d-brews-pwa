function SettingsPage() {
  return (
    <section className="dashboard-placeholder">
      <h2>Settings</h2>
      <p>This is where printer setup, account settings, receipts, and system preferences will be configured.</p>
    </section>
  );
}

export default SettingsPage;